name: Build, Test and Push Docker Image

on:
  workflow_call:
    inputs:
      base_project_name:
        description: 'The base project name'
        required: true
        type: string
      project_name:
        description: 'The project name'
        required: true
        type: string
      short_sha:
        description: 'The short SHA of the commit'
        required: true
        type: string

jobs:
  build-test-push:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout source code
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Fetches full history for better caching/context
          submodules: recursive

      # 2. Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # 4. Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 5. Build Spack Base image
      - name: Spack Base image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./docker/Dockerfile-Spack-Base
          push: true
          platforms: linux/amd64
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/${{ inputs.base_project_name }}:dev-${{ inputs.short_sha }}
          cache-from: |
            type=gha,scope=spack-base
            type=registry,ref=${{ secrets.DOCKER_USERNAME }}/${{ inputs.base_project_name }}:latest
          cache-to: |
            type=gha,scope=spack-base,mode=max
            type=inline

      # 6. Build CATChem image
      - name: CATChem image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./docker/Dockerfile
          push: false
          platforms: linux/amd64
          build-args: |
            BASE_IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ inputs.base_project_name }}:dev-${{ inputs.short_sha }}
          tags: ${{ inputs.project_name }}
          cache-from: |
            type=gha,scope=catchem
          cache-to: |
            type=gha,scope=catchem,mode=max
            type=inline
